@* if SuperVisor*@
@if (User.IsInRole(RolesValues.SuperVisorRole))
{

    <div class="nav_list">
        <a href="@Url.Action("Permissions", "Instructor")" class="nav_link">
            <i class="bx bx-message-square-detail nav_icon"></i>
            <span class="nav_name">Student Permissions</span>
        </a>
        <a href="@Url.Action("Schedule", "Instructor")" class="nav_link">
            <i class="bx bx-table nav_icon"></i>
            <span class="nav_name">Schedule</span>
        </a>
    </div>
}
